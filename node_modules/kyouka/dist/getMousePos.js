/**
 * Get the position of mouse.
 *
 * @param {any} ev
 * @returns {Point}
 */
const getMousePos = (ev) => {
    let x = ev.clientX;
    let y = ev.clientY;
    return { x, y };
};
export default getMousePos;
