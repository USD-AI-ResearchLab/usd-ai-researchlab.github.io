import { Time } from "../types/types";
/**
 * Return the formatted duration with 4 units: day, hour, minute, second.
 *
 * @param {number} seconds
 */
declare const formatDuration: (ms: number) => Time;
export default formatDuration;
