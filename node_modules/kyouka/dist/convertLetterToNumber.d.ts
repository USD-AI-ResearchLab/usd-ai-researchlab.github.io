/**
 * Convert a letter to its corresponding number (such as A->0).
 *
 * @param number
 * @returns
 */
declare const convertLetterToNumber: (letter: string) => number;
export default convertLetterToNumber;
