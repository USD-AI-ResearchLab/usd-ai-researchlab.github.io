/**
 * Creates a function that invokes `fn` with `partials` appended to the arguments it receives.
 * @param fn
 * @param partials
 */
declare const partialRight: (fn: Function, ...partials: any[]) => (...args: any[]) => any;
export default partialRight;
